<!DOCTYPE html>
<html>
  <head>
    <title>Employee Management</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        margin: 0;
        padding: 0;
        background: #fff;
        color: #333;
      }
      a {
        color: #007bff;
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
    </style>
  </head>

  <body>
    <nav style="background: #333; color: white; padding: 1rem;">
      <div style="max-width: 1200px; margin: 0 auto; display: flex; gap: 2rem;">
        <%= link_to "Employees", employees_path, style: "color: white; text-decoration: none;" %>
        <%= link_to "Departments", departments_path, style: "color: white; text-decoration: none;" %>
      </div>
    </nav>
    
    <main style="max-width: 1200px; margin: 2rem auto; padding: 0 1rem;">
      <% if notice %>
        <p style="background: #d4edda; color: #155724; padding: 1rem; border-radius: 4px; margin-bottom: 1rem;"><%= notice %></p>
      <% end %>
      
      <%= yield %>
    </main>
  </body>
</html>
